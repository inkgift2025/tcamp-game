# UI Assets

这里放未来可复用的 UI 图片素材。

推荐文件：

- `ui_panel.png`
- `ui_button.png`
- `pressure_icon.png`
- `score_icon.png`
- 可选：按钮状态、面板边框、警报纹理、图标集

建议尺寸：图标 64x64 或 96x96；面板和按钮可做九宫格或透明 PNG。

当前高级 UI 由 HTML/CSS overlay 负责，这里的素材主要用于后续增强按钮、面板、图标和警报纹理。
